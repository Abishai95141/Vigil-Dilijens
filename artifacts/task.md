# Vigil — Task Checklist

Progress tracker, organized by the phased roadmap (`docs/13-rd-execution-roadmap.md`).
Build order is **risk-driven, not feature-driven**: silent failure modes (identity
mis-joins, false equivalences, unfalsified phenomena, mis-calibrated bands) are
attacked before the features they would corrupt.

**Legend:** `[x]` done · `[~]` in progress · `[ ]` not started · 🔒 gate (must pass to exit phase)

**Mandate:** build robust and complete; test functionality for real as it's built;
never substitute a shallow proxy to appear done. Report outcomes faithfully.

---

## ✅ Phase 0 scaffold (setup) — DONE

- [x] Toolchain on dev Mac: Go 1.26.4, just 1.52, buf 1.70 (uv/node/pnpm/docker/kind present)
- [x] Git repo initialized in `Desktop/vigil/`; remote `Abishai95141/Vigil-Dilijens` (empty, ready)
- [x] Directory tree per techstack §13 layout
- [x] Go module `github.com/Abishai95141/Vigil-Dilijens` (single root module; CGO-free)
- [x] `justfile` — cross-platform command surface (macOS + Linux parity)
- [x] CI: `.github/workflows/ci.yml` (go · proto · clockd · harness) + opt-in `integration.yml`
- [x] `proto/` + buf: clock contract with **no-string-fields** charter rule; committed Go gen
- [x] `obsd/internal/params` — parameters file v0 (doc 14 §5) **implemented + fully tested**
- [x] `obsd` skeleton: `cmd/obsd` (loads params, slog), `cmd/replay` (stub), version pkg
- [x] Internal package tree with `doc.go` contracts (identity, binding, qss, observe, selection, detect, unexplained, clock, graph, api)
- [x] `obsd/internal/clock/conformance_test.go` — asserts clock proto stays string-free
- [x] `clockd/` (uv) — StubClock (honest flat/widening-band behaviour) + tests + vendored SKILL.md
- [x] `harness/` (uv) — backtest primitives (band coverage, time-to-cross) + tests
- [x] `tools/graphlint` — ontology schema validator (green on example release) + tests
- [x] `ontology/schema/graph.schema.json` (v1-skeleton) + `graph/example-oom.yaml` (scaffold example)
- [x] `web/` — Vite/React/TS scaffold + provenance design tokens + vitest (deps install on demand)
- [x] `deploy/` — 3-node kind config + read-only RBAC
- [x] Root + per-subtree `CLAUDE.md`; README; corpus README
- [x] Verified: `go build ./...`, `go vet ./...`, `go test -race ./...`, `just lint`, clockd/harness suites all green

---

## Phase 0a — Foundations  (identity is *prerequisite zero*)

- [x] **01 Charter** ratified — classes, composition rules, prohibited derivations (encoded in CLAUDE.md as review rules)
- [x] **01 M2** Class carriage convention named in component contracts
- [x] **02 M1** Schema v1 — `ontology/schema/kg.schema.json` matches the real KG node/edge shape (10 node + 12 edge types); validates the graph.
- [x] **02 M2** Reference-graph INGESTED: 842-node KG in `ontology/graph/k8s_signal_kg.json`; runtime loader `obsd/internal/graph` (typed, indexed, content-hashed; resolves phenomenon membership from participates_in / relations from phenomenon_relation) — tested against the real KG (842/3759/589, 457 metric).
- [x] **02 M3** Authoring lints: `tools/graphlint` (schema + referential integrity + gap report) wired to `just graphlint` + CI. **Gap report** surfaces the authoring queue: all 38 phenomena missing spans (doc 02 §3.6), 0 structured threshold rules (60 agent hints), 112 data_type variants to normalize; + a curation warning (23 owned_by_agent edges → 2 mistyped Agent names). graphlint `--strict` gates on these before detection ships.
- [x] **02 M2-author** (spans + threshold rules): AUTHORED overlays in `ontology/graph/overlays/` — `spans-v1.yaml` (span + traversal edges + rationale for all 38 phenomena: 17 entity-local / 16 first-order / 5 second-order; control-plane singletons joined by direct role reference, principle in header) and `threshold-rules-v1.yaml` (8 structured rules: config-relative limits/allocatable/PVC-request paths + flagged defaults). Loader (`LoadWithOverlays`) merges deterministically, version pin covers base+overlays; graphlint validates overlays as hard errors and **`-strict` passes (and gates CI)**. Curation: 2 mistyped Agent names fixed (23 edges). REMAINING authoring queue: data_type normalization (112 variants, doc 09 funnel), 60 agent threshold hints to structure, v2 rules (apiserver inflight, etcd fsync, ARP).
- [x] **03 M1** Identity model + CEI scheme — both layers, minting, role derivation; churn tests pass (restart/reschedule/scale/**recreate-same-name honeypot**). `obsd/internal/identity/cei.go`
- [x] **03 M2** Normalization maps — cAdvisor + KSM + node-exporter (`obsd/internal/identity/normalize.go`). All doc 14 §3.2 traps encoded + tested: no-pod-UID time-aware join, `container=""`→pod aggregate, `container="POD"`→deliberate drop, KSM terminated-pod persistence, class-aware container routing, node-name join, **same-name recreate race incl. genuinely-late samples (EventTime vs receive time)**. Quarantine-never-guess; typed reasons; join audit records; versioned maps. Adversarially verified (4 confirmed findings fixed). *OTel semconv = 4th lane, Phase 0b–1 per doc 14 §3.2.*
- [x] **03 M3** Lifecycle state machine + succession + client-go informer wiring (`lifecycle.go`, `resolver.go`, `informer.go`, `cluster.go`). Two-tier tombstones (15m full / 24h stub / 250k LRU cap, evicted-before-horizon metric); time-aware `Lookup` (heap-backed, clock-injected, horizon-enforced at read); succession + same-name honeypot at store level; role chain resolver (RS→Deployment, Job→CronJob, degraded fallback); SharedInformers (pods/nodes/RS/jobs) with `HasSynced` gate; cluster id = kube-system UID (A9). Fake-clientset tested + `-race`; live wiring in `obsd --kubeconfig`; integration test behind `integration` tag. Adversarially verified.
- [x] **03 M4** Timestamped topology edges + validity-intersection contract (`edges.go`, `edges_watch.go`). EdgeStore: assertions with asserted/last-confirmed/retracted stamps; per-type staleness budgets (params); `Traverse(type,from,to,window)` → valid/suspect/absent (pure fn of state+window — replayable; horizon is a GC boundary, not a traversal input); retracted-edge 30m horizon; monotonic reassert. Wiring from informers: runs-on (pod→node, reconciled on reschedule), mounts (pod→PVC), selects (service→pod, **reconciled across ALL slices** — survives EndpointSlice rebalance), node-lease (node liveness via Lease RenewTime). Fake-client + store tests, `-race` (87 identity tests). Adversarially verified: 5 confirmed defects fixed incl. **blocking** slice-rebalance spurious retraction.
- [x] **03 M5** Join-audit tooling + health metrics (`audit.go`, `truth.go`, `metrics.go`). Mis-join detector: `VerifyJoin` + `AuditConsistency` cross-check the lifecycle Store against INDEPENDENT control-plane truth (raw listers, via the Watcher `TruthSource`) — a stale/wrong CEI surfaces as a mis-join (the silent killer), lag as Missing. `EvaluateGate` = Misjoins==0 AND coverage≥target. Prometheus `Collector` (join accuracy, orphan/quarantine rate by reason, edge staleness per type, tombstone tiers, **`phase0a_gate_passed`**). `obsd` serves `/metrics` `/healthz` `/readyz`; inventory loop logs the live gate verdict. Tested incl. mis-join detection + collector gather (99 identity tests, -race).
- [x] **05 M1** Stream conventions + OWN SCRAPER (doc 14 A1): `obsd/internal/observe` parses exposition text from each node's cAdvisor endpoint via the API-server proxy (`kube.ProxyFetcher`, A2 dev path), routes every series through the identity normalizer, stores CEI-stamped samples in the `qss` hot tier (240-sample rings; warm/disk pending, stated). Receive time = audit stamp; sample's own timestamp = join time (A12). Drops/quarantines counted by typed reason; partial scrapes stated. Live: 5785 series/cycle -> 2482 streams, ZERO quarantines.
- [~] client-go SharedInformerFactory wiring — pods, nodes, ReplicaSets, Jobs (03 M3); services + EndpointSlices (selects edge), Leases (node-lease), PVC (mounts edge) (03 M4). ns/PV/Events not separately watched (not needed for current scope; stated).
- [x] First demo: `go run ./obsd/cmd/obsd --kubeconfig <path>` prints a live, correctly-joined entity inventory + join-audit gate verdict (run on the Linux kind box)
- [x] 🔒 **Exit gate RUN + PASSED on the live kind cluster** (M5): `go test -tags=integration -run TestIdentityAgainstLiveCluster` against kind-vigil → **join accuracy=1.0000, coverage=1.0000, misjoins=0, 28/28 checked, passed=true**. (Fixed a pre-existing kube→binding→identity import cycle that had kept this test unbuildable.)

## Phase 0b — Binding & local observation

- [~] **04 M1–M5 (M6 trigger machinery)** — M1 COMPLETE: signal obtainability gating (all 589 signals land obtainable/out-of-scope/indeterminate with reasons; tool detection from workload corpus + NodeInfo; capability classes incl. kernel-version parse + presence-derivable caps; distro gates evaluated, dockershim gate ACTIVE on kind), equivalence resolver (35 groups compiled, ambiguity surfaced never picked), emission metadata per binding. M3 COMPLETE: semantic QA against live stream evidence — character (TYPE + observed shape incl. counter-reset tolerance), scope, range (incl. DERIVED-ratio quantities for divisor rules), platform-variant, ambiguity; seeded false-equivalence fixtures all caught; live progression 64 suspect -> 64 verified/0 failed once evidence accumulated. M5 CORE: per-phenomenon observability (11 full / 12 partial / 15 none on kind, reasons stated) + availability/QA/validation summaries in the rendered coverage report. Availability gating joined into Compile: rules on unobtainable signals land out-of-scope (restarts/KSM, node rules/node-exporter, PVC/CSI on kind) and resolvability counts only evaluable pairs.
  Core compiler (M2/M4, same package): threshold-rule instantiation across entity instances (Container/Node/PVC fan-out from `entity_scope`) + role-layer aggregation (axis 3); per-instance bars from live config (`kube.SnapshotConfig`) with strict precedence (config → default, defaults FLAGGED); eligibility gates; resolvability metric + unbounded list (every pair has a state); re-binding on inventory-fingerprint change or config-drift poll; deterministic. **Live: 11/11 memory bars byte-exact vs kubectl.** REMAINING for full 04: **M6 re-binding reactivity → notify selection (blocked on doc 06)**; the equivalence resolver exists but its full dialect sweep only activates with non-native exporters; v2 threshold rules (apiserver inflight / etcd fsync / ARP).
- [x] **05 M2** Primitive evaluators (`obsd/internal/observe/primitives.go`): threshold (4-rung A6 ladder, direction-aware, NaN-guarded), rate-of-change (5m smoothed first-difference, reset-segmented, gap-breaking, timestamp-sorted per A5), co-occurrence (conjunction). Golden fixtures for crossings/resets/gaps/below-bars; constants in the params file (well_above_factor, at_threshold_band).
- [x] **05 M3** Fingerprint materializer (`obsd/internal/observe/fingerprint.go`): per usable bound (entity,variable) the right primitive by metric character (gauge level / counter→rate→millicores / counter ratio / rate-guard); each component carries ladder/guard state, bar SOURCE (config|default-flagged), staleness vs watermark, Inconclusive-on-gap, and a derivation reference (stream+divisor). Re-materialized every eval tick; deterministic. obsd renders a live fingerprint table. **Live-verified on boutique**: adservice working_set byte-exact vs raw cAdvisor; caught a real kindnet CPU-throttle crossing. **Adversarially verified** (5 lenses → refute-by-default): 4 defects fixed incl. **blocking** rate-guard-goes-silent-on-gap.
- [x] **05 M4** Coverage accounting — per-phenomenon observability (`binding.PhenomenonObservability`): each phenomenon's required members gated against signal availability → full/partial/none with reasons (kind: 11/12/15), feeding the coverage report (04 M5) and detection's degraded-match honesty (07). Done as part of 04 M5.
- [x] **05 M5** Replay interface — **recorded readings replay byte-identically** (the Phase-0b exit property, now executable). qss warm tier (doc 14 §2.3): append-only 2h segments (CRC32C-framed def/sample/tick records preserving exact arrival order interleaved with eval ticks), sealed-immutable, torn-tail crash recovery, 7d retention (no wall clock), 1s batched fsync, latched-loud write errors, closed-latch (no silent re-open). `internal/replay`: bundle (manifest pins graph version + params; bars per content-hashed epoch; segments) + engine re-running it through the REAL Materialize+Matcher, verifying every recorded tick digest; graph-version mismatch refuses; tampered readings DETECTED (negative-tested); corrupt sealed segments refuse. obsd `--store-dir` captures live (store gate: ticks see whole scrape cycles — also fixed a pre-existing eval/scrape race); per-tick digests printed live. `cmd/replay` real (exit non-zero on mismatch) + `-export-parquet` (Python bridge). Harness first deterministic regression (11 M1 exit): pytest runs the binary ×2 on the committed fixture (byte-identical, real findings) + pyarrow Parquet round-trip. **Live: a 200s capture (84,254 samples / 2,584 streams / 3 bars epochs / a real degraded MEMORY_LEAK on currencyservice at 119.1Mi vs 121.6Mi bar) replayed ALL 14 ticks byte-identically, twice.** Live test also caught + fixed a real identity mis-join (containerd pause sandbox arrives container=\"\"+pause-image, was folded into pod streams) and verification caught + fixed a blocking NaN→digest-panic path (non-finite values now refused at the ingest gate, counted; Digest errors instead of panicking). NOTE: adversarial workflow hit the session limit — 1/5 lenses completed (its 8 candidates manually verified: 4 fixed, 3 refuted, 1 positive); re-run the remaining 4 lenses (format-recovery, live-replay-fidelity, digest-blindspots, bundle-trust) after reset.
- [x] **06 M1** Selection funnel (`obsd/internal/selection`): gate 1 coverage (≥1 evaluable bound variable) → gate 2 phenomenon participation (binding rule→signal→participates_in UNIONED with check-metric backing, so the funnel provably covers everything the matcher can fire on); Tier A assigned with reason codes (phenomenon-member / no-phenomenon-member / no-evaluable-variable / structural-entity / bindings-stale); every inventory entity gets a record (none-list published, never silent); Tier B structurally present, EMPTY until Phase 2 (stated). TierASet is a pure function of (bindings, graph) so detection's selection filter replays byte-identically — the engine applies the same per bars epoch; pre-selection bundles verified to replay unchanged. **Live: 28 entities → 16 Tier-A; paymentservice (no limits) named in the none-list as no-evaluable-variable.** Adversarially reviewed (5 lenses incl. the four 05 M5 DNF lenses): 2 confirmed defects fixed (sealed-segment clobber [blocking, executed repro], stale-bindings reason lie) + 12 triaged-real hardening fixes (bundle continuation across restarts w/ run-start frames — **live-verified: capture → kill -9 → restart same dir → replay all ticks byte-identical across the crash boundary**; ResolvedAt-free bars hash; EOF-swallowing read; result-finiteness guards; divisor exposition type; manifest plausibility + hot-ring pin; zero-ticks ≠ pass; total segment order).
- [x] **07 M1** Entity-local matcher (`obsd/internal/detect/match.go`): zeroth-order phenomenon matching over fingerprints — required (mandatory) + supporting (strengthening) member conjunction; FULL vs DEGRADED with completeness score + unobservable members named (doc 07 §3.3); AUTHORED member notes attached verbatim (MEASURED+AUTHORED, never fused); stale/absent/ambiguous/inconclusive = missing; graph-version-pinned; deterministic. Authored `detect-conditions-v1.yaml` (member→fingerprint check, with a min_state materiality guard, doc 07 §3.6) merged+pinned by the loader, graphlint-validated. gauge-slope added to the rate primitive. **Live-verified**: healthy cluster SILENT (zero false positives after the live test caught warmup over-sensitivity → min_state guard), and a deliberately-injected leak pod fired one DEGRADED MEMORY_LEAK (working_set above+rising; pod OOMKilled per ground truth). Adversarially verified (5 lenses): 10 defects fixed incl. gap-slope handling, flagged-bar provenance, unobservable-note drop, duplicate-check collapse.
- [x] **qss** store: hot rings ✓ + warm 2h segments ✓ + 7d retention ✓ + Parquet bundle export ✓ (05 M5) + `modernc.org/sqlite` findings persistence ✓ (doc 14 A7, done in 10 M1, `internal/store`) — all pure Go, CGO-free.
- [x] **brand kit + design system** (`brand/`): provenance-led design system — `tokens.css` single source of truth (MEASURED teal / PROJECTED violet-band / AUTHORED muted-gold reserved; 8pt spacing; Plus Jakarta Sans titles + Poppins body + JetBrains Mono data; severity ladder; coverage states; a11y), `BRANDKIT.md`, watcher's-aperture logo. Wired into `web/` (theme.css imports tokens, self-hosted fonts, brand primitives in `components/Brand.tsx`, branded shell). Researched vs Grafana/Datadog/Honeycomb + 8pt-grid/uncertainty-band literature.
- [x] **10 M1** Coverage-report surface (the first customer-facing screen — honest visibility map): `internal/store` (SQLite findings persistence, modernc.org/sqlite pure-Go/CGO-free, A7, OFF the deterministic path) + `internal/api` (composes binding+observability+selection into a CoverageView, gaps-first, honest unavailable state; serves `/api/coverage` + `/api/findings`) + obsd `--api`/`--db` (publishes snapshot + persists findings each tick, non-gating) + `web/surfaces/CoverageReport.tsx` (the page, on the design system). **Live-verified: /api/coverage returns 28 entities / 16 Tier-A / 11-12-15 full-partial-none phenomena / 60% resolvability / 64 QA-suspect / 14 default bars, per-phenomenon missing reasons stated.** Go suite + web (vitest+biome+tsc) green; CGO-free. Commits e5e9a6c, ada0287.
- [~] **11 M1–M2** Replay substrate ✓ (05 M5: harness ran its first deterministic regression — pytest over the replay binary + fixture bundle + Parquet round-trip); binding-QA suite over replay fixtures + charter battery still pending
- [x] **12 M1** First immutable graph release (`ontology/releases/v0.1.0.yaml`): the graph ships as immutable, semantically-versioned releases (doc 12 §3.1) — the content-hash pin (already wired through binding/detection/replay) is now governed by a declared release. `internal/graph`: Release type + VerifyRelease (rejects a drifted graph LOUDLY — immutability enforced) + IdentifyRelease (graph self-IDs its release by hash) + LatestRelease. Immutability GATE = `release_test.go` (latest release must equal live graph; older = historical pins) + `graphlint -release` folded into `just lint`; `just graph-version` cuts releases. obsd logs `release=v0.1.0`, surfaces `graphRelease` on /api/coverage + the web header. **Live: obsd loaded v0.1.0 (842 nodes/3759 edges/8 rules/3 overlays); API carries graphRelease=v0.1.0.**
- [x] 🔒 **PHASE 0b EXIT — CLOSED.** deterministic replay byte-identical ✓ (live capture replayed 14/14 ticks, twice, incl. across a kill -9 restart) · binding QA fixtures pass ✓ · resolvability/coverage metrics published ✓ + surfaced (10 M1) · entity-local phenomena fire on replay fixtures ✓. All Phase-0b milestone boxes done: 04 (M1/M2/M4/M5), 05 (M1–M5), 06 M1, 07 M1, 10 M1, 11 M1, 12 M1, qss, brand kit. Phase-0a gate also certified live (accuracy 1.0). Carry-forward (Phase 1): 04 M6 re-binding reactivity, 11 M2 binding-QA-over-fixtures + charter battery, and re-running the 4 adversarial lenses that hit the session limit (05 M5/06 M1: format-recovery, live-replay-fidelity, digest-blindspots, bundle-trust — candidates manually triaged, but a clean automated pass is owed).

## Phase 1 — Topological detection

- [x] **Phase-1 prep: node-exporter live** — DaemonSet (`deploy/workloads/node-exporter.yaml`, pinned v1.9.1, hostNetwork, all 3 nodes incl. control-plane); scraper grew a second exposition family (port-qualified node proxy `nodes/<n>:9100/proxy/metrics`; per-payload Family routing; node-exporter stream IDs carry the full sorted label set so multi-series families like node_filesystem never interleave in one ring — single-series target metrics keep clean IDs). Tool detection flipped node signals obtainable automatically (corpus-based, nothing configured). **Live: 6 endpoints, 7,915 series/cycle, zero quarantines; node MemAvailable bars resolve config-sourced (allocatable×0.10); QA 97 verified / 3 failed** — the 3 failures are the honestly-ambiguous node_filesystem ratio (7 mountpoint streams; sub-variable normalization is the doc 02 data_type queue). Live-caught + fixed QA defect: a divisor-less counter under a ratio bar (PSI stall-seconds) was range-checked on its cumulative LEVEL (~1008s vs 1.5) instead of its rate — rate-evaluated quantities now range-check Δ/Δt (regression-tested).
- [x] **07 M2–M6 — ALL DONE.** **M2 first-order spans with the edge-validity contract:** matcher evaluates spanned phenomena at their AUTHORED anchor only; neighbour members across declared edge types one hop (Container anchors walk from their pod — containment is identity, not a hop); absent edge never contributes (degrade-never-fabricate, golden-tested incl. window-intersection: retracted-inside-window supports, before/after = absent); suspect edge degrades + is NAMED; findings carry span instantiation (edge path + validity verdicts + per-neighbour evidence rows). **Anchor-evidence rule (live-caught): a match requires ≥1 required member met ON THE ANCHOR** — neighbour evidence corroborates, never alone constitutes (before: every container on a pressured node fired "degraded cascade" on node PSI alone). **Live: chaos `cpu-cascade.yaml` → PHEN_THROTTLING_CASCADE FULL 100% at the throttle-victim; healthy cluster SILENT (§3.5 live).**
  **M3 second-order (two-hop) spans:** generalized to declared depth 2 — best-path-per-entity reach sets (hop1/hop2, both directions, declared edge types only, hop-1 entities excluded from hop-2, visited-excluded); path-aware hits whose verdict is the path's WORST step (a suspect hop ANYWHERE degrades + is named; an absent hop ANYWHERE contributes nothing — a broken FIRST hop makes the two-hop entity unreachable and its real evidence never fabricates); multi-step SpanPath as-walked from the anchor; evidence carries Hop + joined Via ("runs-on→mounts"). Golden fixtures: STORAGE_SATURATION node→pod→PVC on the real graph; the doc's noisy-neighbour A→node→B shape via a TEST-only overlay (real KG members, synthetic metrics); broken-first-hop never fabricates; hop-1 entity never re-listed at hop-2; snapshot round-trip identical at depth 2. **Live (chaos `io-pressure.yaml`): fsync writers on worker2 → PHEN_STORAGE_SATURATION DEGRADED second-order at the node — IO PSI well-above its 0.10 flagged bar, the per-device disk-IO required member honestly named unobservable (device sub-variables = doc 02 data_type queue); 165 live findings, 387 in the byte-identical replay.**
  **M5 cascades + blast radius:** `detect/cascade.go` — relations normalized into trigger→downstream (BOTH authored shapes: role=downstream forward, role=trigger reversed); deterministic windowed CascadeTracker (latest ref per (phen,entity), pruned, RESET at run boundaries — process restart empties it, engine resets at FrameRunStart so cascades replay byte-identically); RecognizeCascades pairs a downstream finding with an in-window trigger on a TOPOLOGICALLY RELATED entity (same-entity / containment / ≤1 valid hop — never a story across an absent edge); trigger never follows downstream. Blast radius = downstream relations × selection participation × valid topology, made concrete (authored relationship, never a prediction). Cascades joined into the replay digest (TickResult.Cascades); golden tests: leak→OOM across ticks, window expiry, same-tick, topological-relation-required, both-shapes resolution, radius needs selection+topology, determinism. **Live blast radius CONFIRMED (50 entries): the MEMORY_LEAK finding names itself (same-entity) AND its node (runs-on) at risk of PHEN_OOM_KILL_CGROUP, authored note "Eventual outcome"/T0+terminal verbatim — the at-risk set BEFORE the kill.** Replay fixture regenerated under v0.3.0 carries the leak→OOM cascade + the storage two-hop and replays 100% byte-identical (149 live ticks; harness asserts the cascade in canonical output + 5-stream Parquet).
  **HONEST live-cascade gap (well-understood, NOT a logic defect):** on this containerd kubelet `container_oom_events_total` reads 0 on every 15s scrape even right after an `exit:137` OOMKill — the counter dies with the torn-down cgroup before the next scrape, so OOM_KILL_CGROUP doesn't fire LIVE and the live leak→OOM *cascade* never closes (the leaker OOM-killed 4× during the soak; 25 MEMORY_LEAK findings + 50 blast-radius lines fired regardless). Cascade recognition itself is proven byte-identically in the replay fixture. **Carry-forward:** ingest KSM `kube_pod_container_status_last_terminated_reason`=OOMKilled (a DURABLE OOM signal) as the OOM_KILL_CGROUP member so the live cascade closes. **Curation note (for governance):** the KG marks `node_vmstat_oom_kill` corroborating-only on OOM_KILL_CGROUP ("ONLY on system OOM") — correct; cgroup OOMs need the cAdvisor/KSM lane, not vmstat.
  **M4 DONE — degraded-match policy completion:** `detection.min_completeness` (the doc 07 §3.6 span-completeness threshold for degraded surfacing) + `detection.cascade_window` in the params file, BOTH digest-bearing and pinned in the replay-bundle manifest (a capture replays under ITS regime; old bundles fall back to the co-occurrence window, stated); a FULL match is never floor-gated; checked-but-unobservable SUPPORTING members are now NAMED gaps on the finding (SupportingGaps/SupportingUnobserved — previously silently mis-counted as observable; quality stays keyed to required coverage + path validity, the stated policy); the binding-time "this phenomenon can only ever match DEGRADED on this cluster" pre-declaration (04 M5 observability) is JOINED AT THE SCREEN on degraded findings (charter: adjacent at the surfacing layer, never inside the deterministic finding).
  **M6 DONE — sensitivity calibration (with 11 M4):** replay EVALUATION mode (`replay -eval -eval-band/-eval-min-completeness/-eval-well-above/-eval-cascade-window`) re-evaluates a bundle's recorded inputs under an overridden regime — digests are NOT compared (they describe the capture regime; comparing would be a meaningless mismatch dressed as a verdict), the report says EVALUATION loudly, implausible overrides refuse — closing the manifest's long-stated "time-shifted evaluation" absence. `harness/src/harness/sensitivity.py` sweeps a grid and scores event-level precision/recall against ground-truth labels (`corpus/labels/bundle-v1.labels.json` for the committed fixture; definitions in the module). **Evidence (corpus/labels/calibration-07M6.md): pinned defaults P=1.0 R=1.0 cascade 1/1; band 0.10 → P=0.938 (premature at-threshold claims — the argument for 0.05); ANY positive completeness floor kills recall on sparsely-checked phenomena (0.25 already suppresses OOM_KILL_CGROUP at 1/8 checkable and WITH IT the leak→OOM cascade, 0/1) while precision was already 1.0 — the argument for floor 0 (the anchor-evidence rule + conjunction + edge-validity carry the FP defence); cascade_window 10m made explicit.** Ordering strictness (T0− before T0) deferred with the dependency named (member-state onset tracking). 4 sweep regressions in `harness/tests/test_sensitivity.py` pin the evidence shapes. Standing caveat: the corpus is small; per-class Phase-1-exit targets need the fuller 11 M3 corpus and re-derivation as it grows.
- [~] **06 M2,M4,M6** — **M2 DONE: neighbourhood closure.** Selection expands every Tier-A entity to the spans of its participating phenomena (declared edge types, declared depth 1–2, both directions, validity-honoured, over the SAME per-tick snapshot detection walks); each NeighbourRef carries edge type/verdict/hop/for-phenomena; unreachable neighbourhoods are RECORDED gaps (never silent). **Live: 22 Tier-A (16 before node signals), 47 neighbour refs, gaps enumerated.** M4 (criticality/operator scope) + M6 (re-selection reactivity) pending.
- [x] **Topology replay substrate (07 §3.7 / closes the manifest's stated `Absent: topology log`)** — `identity.EdgeStore.Snapshot()` (canonical order) + `NewEdgeStoreFromSnapshot` + `NeighboursInto`/`Sources` (reverse walks, deterministic order on BOTH live and rebuilt stores); each tick captures its topology snapshot in the TickRecord and **evaluates against the snapshot-rebuilt store, never the mutating live store — recorded == evaluated by construction**; manifest pins per-edge-type staleness budgets (suspicion replays under the capture's budgets, never the local params); pre-topology bundles replay entity-local-only, stated in the report (`TopologyLess`); a budget-pinned bundle with a topology-less tick REFUSES (corrupt, never mis-verified). Fixture regenerated under v0.2.0: carries the first-order cascade and replays it byte-identically (harness regression asserts the cascade in canonical output + 4-stream Parquet round-trip).
- [x] **12 (routine release)** — graph release **v0.2.0** cut through governance: threshold-rules-v2 (THR_NODE_CPU_PSI_STALL 0.10 flagged; THR_NODE_OOM_KILL_BURST ≥1/15m flagged) + detect-conditions-v2 (anchors + `on: anchor|neighbour` member checks for THROTTLING_CASCADE / EVICTION_MEMORY / CONNTRACK_EXHAUSTION / OOM_KILL_SYSTEM — all checks bind EXISTING KG members, referentially linted). Loader + graphlint both validate the new vocabulary incl. cross-overlay coherence (span/anchor/neighbour may arrive from different files → finalize pass); immutability gates green. Full 12 M2–M4 (change classes, review workflow, regression gates) pending.
- [x] **12 (routine release)** — graph release **v0.3.0** cut through governance: threshold-rules-v3 (THR_NODE_IO_PSI_STALL 0.10 flagged; THR_CONTAINER_OOM_EVENTS ≥1/15m flagged) + detect-conditions-v3 (`on: two-hop` vocabulary; STORAGE_SATURATION Node anchor with two-hop PVC member; OOM_KILL_CGROUP Container anchor — closes the MEMORY_LEAK→OOM relation). Loader + graphlint validate the two-hop vocabulary incl. the two-hop⇒second-order coherence (finalize pass); immutability gate green.
- [x] **07 COMPLETE (M1–M6)** — entity-local, first-order, second-order, degraded policy, cascades + blast radius, calibrated sensitivity. Remaining 07-adjacent work lives with its owners: KSM OOM lane (live cascade), member-onset tracking (ordering strictness), corpus growth (11 M3).
- [x] **08 M1–M4** Unexplained anomaly channel (`obsd/internal/unexplained`) — the blind-spot patch, honestly bounded. **M1 loudness:** the two-clause definition (doc 08 §3.1) over fingerprints — a bar crossing (above/well-above) or a rate excursion (breached guard), the SAME bar/guard vocabulary detection uses (mirrors Fingerprint.Crossed); stale/inconclusive never qualify; no distribution distance, no novelty score. **M2 routing + dedup:** coverage-check against detect findings (anchor AND neighbour members, so a node's loud PSI is "covered" by a container's first-order cascade that consumed it); a loud entity with uncovered loud states routes as "anomalous — investigate"; a stateful Tracker keyed by (entity, uncovered-metric-set) ages persistence into ONE card (not a stream), supersedes-on-match with a link to the covering phenomenon (the unexplained became explained by AUTHORED knowledge, not inference), and resolves when the loudness clears; PARTIAL coverage routes only the remainder; reset at run boundaries (process restart) so routing is part of the digest and replays byte-identically. **M3 surface card** (Scope/LoudStates-with-provenance/MatchCheck/Status/Mark): renderer + `/api/unexplained` (open cards + candidates + blind-spot); the residual blind-spot notice (§3.2 — signals with no bar/guard can never be loud) surfaced REFLEXIVELY; **charter language audit test** scans every emitted string for causal vocabulary (because/caused/triggers/reason/explains/…) — the absence of a reason is the point, enforced. **M4 curation feedback:** recurrence aggregation (signal-set × entity-kind signature) → candidate-phenomenon reports for human curators via governance (doc 12) — recurring across TIME or ENTITIES is each a growth signal; OFF the deterministic digest path (accumulates across runs, like the findings store); the system PROPOSES, it never authors. Wired into the digest (TickResult.Unexplained), engine (tracker reset at FrameRunStart), binder, main tick, renderer, and API snapshot. Comprehensive unit suite (loudness definition, new→aging, resolve, supersede-by-match, covered-never-routes, neighbour-coverage, partial-coverage-remainder, determinism+reset, charter audit, candidate reports). Fixture regenerated (digest gained the unexplained field); replay byte-identical. **MEASURED, conspicuously NOT authored.**
- [x] **10 M2–M4** Operator surfaces — the join, each class still wearing its label (doc 10 §2). **M2 insight feed:** `/api/insights` (BuildInsights — live per-tick snapshot of matched phenomena: MEASURED match + AUTHORED member/relation notes kept as SEPARATE labelled fields, never fused; full/degraded, span class, members with bar provenance, span path + verdicts, suspect edges, blast radius, cascade stories) + `web/src/surfaces/InsightFeed.tsx` (cards with quality/span badges, expandable drill-down to fingerprint evidence, the authored note rendered "per the graph" in AUTHORED dotted-gold beside the measured state). **M3 topology view:** `/api/topology` (BuildTopology — nodes with current marks [matched/loud/selected], edges with validity verdict at `now` mirroring EdgeStore.Status; dangling edges excluded, render cap stated) + `web/src/surfaces/TopologyView.tsx` (deterministic layered SVG grouped by kind, matched=teal ring / loud=amber dashed / selected=azure dot — CURRENT marks only, predictive a separate language deferred to M5/Phase 2; valid=solid / suspect=dashed edges; per-node drill-down). **M4 unexplained surface + timeline:** the unexplained snapshot rendered (MEASURED-only cards, aging/supersede status, blind-spot notice prominent, candidate-phenomenon reports) + durable unexplained persistence in SQLite (`store/unexplained.go` UpsertUnexplained/ActiveUnexplained) + `/api/timeline` (BuildTimeline composing match intervals + unexplained aging spans; the PROJECTED lane RESERVED and stated-empty until Phase 2 — never a fabricated future) + `web/src/surfaces/{UnexplainedSurface,Timeline}.tsx`. App shell gained tab nav (Insights|Topology|Unexplained|Timeline|Coverage); shared `surfaces/types.ts` + `useApi` poll hook; new surface CSS on the design system (quality/status badges, authored-note rule, topology marks, edge validity, timeline lanes). Go builders fully unit-tested (full/degraded counts, span classification, AUTHORED-note separation, blast radius, edge validity, dangling exclusion, truncation, timeline lanes + projected note, store upsert/read). Web gates green (biome clean, tsc clean, vitest, vite build). **Live on kind: all four endpoints serve real data — topology 31 nodes/edges (3 loud node-exporter throttling, all valid), unexplained 3 cards, timeline 3 spans + reserved projected lane; insights correctly empty when the only loud activity is unmatched (routed to unexplained), and populated with the first-order THROTTLING_CASCADE + members/span-path/blast-radius once the chaos cascade fired.**
- [ ] **11 M3–M4** Falsification corpus v1 (chaos runs; `corpus/chaos/` has cpu-cascade, leak-oom, io-pressure) + phenomenon precision/recall suite; sensitivity sweeps · **11 M2 carry-forward** binding-QA-over-fixtures + charter battery
- [ ] **12 M2–M4** Change classes + review workflow; harness-wired regression gates; migration + binding diffs
- [ ] **Live-cascade carry-forward** ingest KSM `last_terminated_reason`=OOMKilled (durable OOM signal) so OOM_KILL_CGROUP fires live (containerd's `container_oom_events_total` is scrape-transient)
- [ ] **06 M4,M6** Criticality weighting + operator scope; re-selection reactivity
- [ ] **04 M6** Re-binding reactivity · **02 M4** evidence linkage in the graph
- [ ] Known cost (stated): per-tick topology snapshots add ~25KB/tick to the bundle at dev scale; a confirmation-stamp-aware dedup is a follow-on, not a correctness issue
- [ ] 🔒 **Exit:** phenomenon precision/recall meets per-class targets; **stale-edge fixtures degrade, never fabricate** (✓ already golden-tested at the matcher; corpus-level fixtures pending); sensitivity defaults evidence-backed; a behavioural-class graph change landed through full governance (v0.2.0 landed through the M1 release gate; the M2–M4 workflow itself pending)

## Phase 2 — Marquee forecasting

- [ ] **09 M1** Clock interface + reference adapter (TimesFM 2.5); swap test with stub passes; conformance fixtures
- [ ] **09 M2** Eligibility funnel over Tier-B + projection against resolved bars; guardrails + silences
- [ ] **09 M3** 🔒 Backtest calibration gate (band coverage + time-to-cross error per target class)
- [ ] **09 M4** First warning class live — container working-set → OOM, blast radius attached
- [ ] **06 M5** Tier-B budgeting (clock invocation ceiling)
- [ ] **10 M5–M7** Early-warning cards + predictive marks; context windows; chat (adversarial register fixtures)
- [ ] **11 M5–M6** Forecast backtest suite operative; charter battery complete
- [ ] **12 M5–M6** Staged rollout + rollback exercised (seeded bad release caught at canary); curation loop closed end-to-end
- [ ] `clockd` TimesFM behind `model` extra — pin package version AND checkpoint revision (doc 14 A15); gRPC serving wired
- [ ] 🔒 **Exit:** backtest gate passes for every shipped warning class **before any operator sees a warning**; register audit clean; seeded bad release rolled back in exercise

## Phase 3 — Decomposition

- [ ] **09 M5** Context-window splice points; per-event-class footprint models (level shift, transient, ramp); abort criterion
- [ ] **10 M6–M8** Context-window + configuration surfaces complete; chat complete; mobile/on-call
- [ ] 🔒 **Exit:** decomposition improves backtest error without degrading band coverage

## Phase 4 — Known-future covariates

- [ ] **09 M6** Covariate admission (scheduled jobs, calendars); per-class lift evaluation
- [ ] 🔒 **Exit:** covariate targets beat univariate baselines in backtests

## Phase 5 — In-context refinement (optional, hard-gated)

- [ ] **09 M7** Inference-time example conditioning — only if a loadable in-context checkpoint is confirmed for the deployed model line

---

## Cross-cutting follow-ons (tracked so nothing is a silent gap)

- [ ] `graphlint`: referential-integrity + authoring-invariant lints (doc 02 §3.6 — member.variable resolves, relation targets resolve, span↔traversal consistency, defaults flagged, untested-status visibility)
- [ ] `just tools`: pin + adopt gofumpt, golangci-lint, govulncheck in CI (currently go vet + gofmt only)
- [ ] Web CI job once the app exists (Phase 0b); install deps + biome + vitest
- [ ] Staging: k3s on 3 small VMs for node-pressure realism (Phase 1 line item, doc 14 §3.1)
- [ ] Add `prometheus/client_golang` self-metrics (join accuracy, orphan rate, edge staleness, Tier-B budget use) + pprof in dev
- [ ] `prod` params profile (5 min reconcile + confirm-on-demand probe)
- [ ] Replay-bundle anonymization standards before any customer capture (doc 14 A16)

## Open-question ledger (doc 13 §6) — resolve with harness evidence, not intuition

1. Per-edge-type staleness budgets; tombstone horizon (initial values in params; refine from churn data)
2. Suspect-binding participation policy (use-but-mark vs hold-out)
3. Degraded-match representation (qualitative mark vs completeness score)
4. Two-hop traversal ceiling; cascade window scaling per edge type
5. Sensitivity-tuning exposure to operators vs held policy
6. Recurrence thresholds + cross-customer aggregation for curation intake (data-isolation review)
7. Forecast surfacing thresholds (max band width, min horizon)
8. Horizon/cadence defaults per target class
9. Which graph events beyond context windows are clean splice points (most gating for Phase 3)
10. Histogram-quantile targets in scope at all
11. In-context checkpoint availability on the deployed model line (Phase 5 hard gate)

---

### ▶ Immediate next step
**Identity layer (doc 03) is COMPLETE — M1–M5 done, 100 tests, pushed.** Three ways forward:

1. **Certify the Phase-0a exit gate on the Linux kind box** (closes Phase 0a): `just up`,
   deploy the Online Boutique workload, then `go test -tags=integration -run LiveCluster
   ./obsd/internal/identity/` — asserts **zero mis-joins** and reports join accuracy/coverage.
   Or watch live: `go run ./obsd/cmd/obsd --kubeconfig ~/.kube/config` → `curl :9095/metrics | grep phase0a_gate_passed`.
2. **Phase 0b — binding (04) + observation/qss (05)**: compile the ontology onto the bound
   cluster, the three primitives + fingerprints, the thin time-series store, the coverage report.
   (Needs the ontology graph loaded — see option 3.)
3. **Ontology ingestion (02 M2)**: load the authoritative 842-node KG (`~/Desktop/vigil copy/k8s_signal_kg.json`)
   — reconcile the schema to its node/edge shape, build the `internal/graph` loader, upgrade graphlint,
   produce the gap report (spans + threshold rules to author). This unblocks binding (04) + detection (07).
