[project]
name = "abb-failure-simulator"
version = "0.1.0"
description = "Self-serve failure-simulation console for the abb-genix cluster, observed in Vigil."
requires-python = ">=3.10"
dependencies = [
    "streamlit>=1.39",
    "pandas>=2.0",
]

[tool.uv]
package = false
