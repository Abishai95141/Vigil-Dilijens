# Build output
/bin/
/graphlint
*.test
*.out
coverage.txt
coverage.html

# /graphify knowledge-graph build output (generated artifact, not source)
/graphify-out/

# Runtime data (qss segments, sqlite findings, graph snapshots, replay bundles)
/data/
*.sqlite
*.sqlite-*
*.db

# Python (uv-managed; lockfiles ARE committed)
__pycache__/
*.py[cod]
.venv/
.pytest_cache/
.ruff_cache/
.mypy_cache/

# Node / web console (pnpm; lockfile IS committed)
node_modules/
dist/
.vite/
*.tsbuildinfo
# the archived dummy testing UI (preserved in git history under web/, kept locally only)
web-legacy/

# Local kube context
*.kubeconfig

# OS / editor
.claude/
.DS_Store
*.swp

# NOTE: proto/gen/ is intentionally NOT ignored — generated code is committed
# (techstack §2.6) so it stays greppable.

# Phase A flow-lane collector build artifact (built via just/docker, not committed)
deploy/flow/conntrack-agent

# Local run scratch: replay/findings SQLite DBs from manual cluster testing
_run/

# Personal capture artifacts (screenshots / recordings — not source)
captures/
captures.zip
